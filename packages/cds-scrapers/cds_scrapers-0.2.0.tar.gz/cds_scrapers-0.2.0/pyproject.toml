[tool.poetry]
name = "cds-scrapers"
version = "0.2.0"
description = ""
authors = ["albertomerango <alberto.marengo@coeuraj.com>"]
readme = "README.md"
packages = [{include = "cds_scrapers"}]

[tool.poetry.dependencies]
python = "^3.9"
pytest = "^7.1.3"
pandas = "^1.5.1"
requests = "^2.28.1"
textstat = "^0.7.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
