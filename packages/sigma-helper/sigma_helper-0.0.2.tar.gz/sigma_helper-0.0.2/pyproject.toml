[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sigma_helper"
version = "0.0.2"
authors = [
  { name="sigmoid", email="ajitg@sigmoidanalytics.com" },
]
description = "data quality helper functions"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",

]
