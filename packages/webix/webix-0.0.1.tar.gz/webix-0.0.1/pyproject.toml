[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "webix"
description = "Linux for the web"
readme = "README.rst"
requires-python = ">=3.8"
keywords = []
classifiers = []
dynamic = ["version"]


[tool.setuptools]
packages = ["webix"]

[tool.setuptools.dynamic]
version = {attr = "webix.__version__"}
