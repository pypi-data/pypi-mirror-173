[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ortools_vrp_wrapper"
version = "0.0.1"
dependencies = [
    'dataclasses-json',
    'ortools',
]
license = {file = "./LICENSE.txt"}