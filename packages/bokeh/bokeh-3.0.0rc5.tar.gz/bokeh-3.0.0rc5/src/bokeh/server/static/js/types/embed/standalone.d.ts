import { Document } from "../document";
import { View, ViewManager } from "../core/view";
export declare const index: {
    [key: string]: View;
};
export declare function add_document_standalone(document: Document, element: HTMLElement, roots?: (HTMLElement | null)[], use_for_title?: boolean): Promise<ViewManager>;
//# sourceMappingURL=standalone.d.ts.map